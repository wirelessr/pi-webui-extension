{
  "name": "pi-webui-monorepo",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "biome check packages/"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3"
  }
}
