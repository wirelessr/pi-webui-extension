{
  "name": "pi-webui-hub",
  "version": "0.6.3",
  "private": true,
  "type": "module",
  "description": "Central hub: single web entry that reverse-proxies and aggregates all pi WebUI sessions",
  "dependencies": {
    "@wirelessr/pi-webui-components": "^0.6.3"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "lint": "biome check src/ test/"
  }
}
